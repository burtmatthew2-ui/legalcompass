// Temporarily disabled - was causing mobile preview issues
interface FloatingAIButtonProps {
  topicContext: string;
}

export const FloatingAIButton = ({ topicContext }: FloatingAIButtonProps) => {
  return null; // Disabled for now
};
